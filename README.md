🌟 معرفی پروژه
این پروژه یک اسکریپت **Shell** است که فرآیند **پشتیبان‌گیری (Backup)** از دیتابیس (DB) و تنظیمات پنل **3X-UI** را خودکار از طریق api به یک **ربات تلگرام** به شناسه یا کانال دلخواه شما ارسال می‌کند.
با استفاده از این ابزار، می‌توانید با خیال راحت از داده‌های کاربران و تنظیمات پنل خود در بازه‌های زمانی (هر یک ساعت) نسخه‌ی پشتیبان تهیه کنید و آن‌ها را مستقیماً در بستر امن تلگرام ذخیره نمایید.

ویژگی ها :
امکان استفاده از پروکسی برای دسترسی به api پنل در صورت لزوم
امکان استفاده از پروکسی برای ارسال فایل بکاپ به ربات تلگرام یا گروه تلگرامی
امکان ارسال فایل پشتیبان به چندین ادمین و چندین گروه یا ربات
امکان مشخص کردن نام فایل و توضحیات به فایل بکاپ
تشخیص خودکار ای پی سروری که روی ان ربات بکاپ اجرا شده و اضافه کردن ای پی به توضیحات فایل بکاپ
امکان ادیت ربات بعد از کانفیگ برای تعغیر یا اضافه کردن... (بعد از تعغیرات برای اعمال تعغیرات ربات را ری استارت کنید )

🚀 نصب و اجرا (Installation)
برای اجرای ، کافیست دستور زیر را در ترمینال سرور خود کپی و اجرا کنید:

```bash
bash <(curl -Ls https://raw.githubusercontent.com/110-ghost/tg-bot-backup-3x-ui/main/manage.sh)
```
 
توضحیحات تکمیلی :

میتونید از ادرس لوکال استفاده کنید اگر روی همان سروری که پنل هست ربات ارجا شده مثال  https://127.0.0.1:2053/ghost
اگر ربات کار نکرد در زدن ادرس پنل دقت کنید / اخر نباید داشته باشه و بعد از ادرس و پورت و پچ پنل نباید هیچی باشه 
برای برسی لاگ و عیب یابی ادرس فایل ربات در پوشه ی /mnt/bot با توجه به اسم انتخابی اسم ربات فایل ربات در این ادرس وجود داره و با دستور python3 اسم ربات میتونید رباتو دستی اجرا کنید و چک کنید مثال python3 ghost.py
اگر از ساکس که روی پنل ساختید برای پروکسی ربات استفاده میکنید میتونید از ادرس لوکال 127.0.0.1 استفاده کنید و بهتر است داختل پنل قسمت Listen IP را روی 127.0.0.1 قرار بدید که از خارج از سرور به پروکسی دسترسی نباشه و امنیت سرور به خطر نیافتد

```
=====================================================
  .-.                 ____ _   _  ___  ____ _____    
 (o o) boo!          / ___| | | |/ _ \/ ___|_   _|   
 | O \              | |  _| |_| | | | \___ \ | |     
  \   \             | |_| |  _  | |_| |___) || |     
   '~~~'             \____|_| |_|\___/|____/ |_|     
-----------------------------------------------------
       3x-ui Telegram Backup Bot by GHOST            
=====================================================
1) Install a new bot
2) Uninstall a bot
3) Edit a bot's configuration
4) Manage a bot (start/stop/logs)
5) Exit

Select an option [1-5]:
```
```
1) Show current configuration
2) Manage bot tokens (add/remove)
3) Manage chat IDs (add/remove)
4) Edit proxy (host/port/user/pass)
5) Edit panel URL
6) Edit panel username
7) Edit panel password
8) Edit server display name (CUSTOM_NAME)
9) Toggle proxy usage for PANEL (USE_PROXY_FOR_PANEL)
10) Toggle proxy usage for TELEGRAM (USE_PROXY_FOR_TELEGRAM)
11) Rename config filename (also renames systemd service)
12) Restart bot
13) Back
Select an option:
```
🤝 مشارکت (Contribution)
از هرگونه مشارکت و ارتقاء این پروژه استقبال می‌شود. لطفاً یک Pull Requestارسال کنید.

⚠️ هشدار

فایل بکاپ اطلاعات مهمی هم چون کانفیگ کاربران ادرس پنل پچ پنل و یوزرنیم پسورد پنل هست و در اختیار دیگران قرار ندهید 
همیشه از تنظیمات و داده‌های خود backup بگیرید قبل از اجرا اسکریپ 
 این پروژه تست شده و برای استفاده از سالم بودن فایل بکاپ زمان بندی ارسال موفق و.. رو برسی کنید 
